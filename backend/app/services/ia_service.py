def procesar_consulta(mensaje: str):
    # 1. anonimizar
    # 2. buscar contexto (RAG)
    # 3. generar respuesta

    return {
        "respuesta": "Esto es una respuesta simulada",
        "alerta": False
    }